# Core bot framework.
# WARNING: there is a separate PyPI package simply called "telegram".
# If that package is installed it shadows this one and causes:
#   ImportError: cannot import name 'Application' from 'telegram'
# The Dockerfile explicitly runs `pip uninstall -y telegram` before this
# install to prevent that conflict.
python-telegram-bot==21.10

fastapi==0.115.6
uvicorn==0.32.1
requests==2.32.3
telethon==1.38.1
httpx
